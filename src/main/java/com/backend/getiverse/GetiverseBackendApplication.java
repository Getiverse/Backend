package com.backend.getiverse;

import com.backend.getiverse.model.support.CheckFile;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GetiverseBackendApplication {
	public static void main(String[] args) {
		SpringApplication.run(GetiverseBackendApplication.class, args);
	}
}
